'use strict'

const binaryTree = require('../binaryTree')
const node = require('../Node')
const fizzBuzz = require('../k-ary-tree')

describe('K-ary-tree',()=>{
    it('FizzBuzz',()=>{

        let result = ['Buzz','FizzBuzz','2','Fizz']

        expect(result).toEqual(result)
    })
})